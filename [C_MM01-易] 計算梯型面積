#include <iostream>
using namespace std;

int main(void)
{
	float a, b, c;
    cin >> a >> b >> c;
    cout.setf(ios::fixed);
    cout.setf(ios::showpoint);
    cout.precision(1);
    cout << "Trapezoid area:" << (a+b)*c/2.0 << endl;
	return 0;
}
