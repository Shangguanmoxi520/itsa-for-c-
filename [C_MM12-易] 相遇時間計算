#include <iostream>
using namespace std;

int main(void)
{
	double a;
    cin >> a;
    a /= 0.238;
    int digit  = ((int)(a*10))%10;
    a -= digit*0.1;
    if(digit != 0) a += 1;
    cout << (int)a << endl;
	return 0;
}
